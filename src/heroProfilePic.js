/** Hero portrait — single URL for <img>, preload, and prefetch. */
export const HERO_PROFILE_PIC_SRC = encodeURI(
  "/assets/Profile Pic no Flyaways.png",
);

/** Compressed square JPEG for Open Graph / iMessage (hero PNG is too large for crawlers). */
export const OG_IMAGE_SRC = "/assets/og-image.jpg";
