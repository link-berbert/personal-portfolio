# Used at build time for Open Graph / Twitter link previews (absolute og:image URL).
# Copy to env/.env (see .gitignore) and set to your live origin with no trailing slash.
# Example: VITE_SITE_ORIGIN=https://lincolnberbert.com
VITE_SITE_ORIGIN=
