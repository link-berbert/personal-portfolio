# Optional: override the canonical + og:url host (defaults to https://www.lincolnberbert.com).
# Use for staging previews. No trailing slash.
# VITE_SITE_ORIGIN=https://preview.example.com
VITE_SITE_ORIGIN=

# Web3Forms (https://web3forms.com) — free tier, emails you on submit. Add the same name in
# Vercel → Environment Variables (then redeploy). Restrict allowed domains in Web3Forms.
# VITE_WEB3FORMS_ACCESS_KEY=
